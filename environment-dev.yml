# environment-dev.yml
name: ds-dev-env
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip
  - pip:
      - black
      - flake8
      - flake8-docstrings
      - flake8-bugbear
      - isort
      - mypy
      - bandit
      - pre-commit